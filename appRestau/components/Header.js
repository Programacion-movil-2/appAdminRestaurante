import React from "react";
import{
    View,
    Text
} from 'react-native';
import{ FONTS } from "../constants";

const Header = ({containerStyle, title}) => {
    return (
        <View
            style = {{
                flexDirection: 'row',
                ...containerStyle
            }}
        >
            {/* Left */}

            {/* Left */}

            {/* Left */}

        </View>
    )
}